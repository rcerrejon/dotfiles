#!/bin/sh

# All-in-One Atom Package for React Native developing
apm install autoclose-html highlight-selected language-babel linter linter-eslint pigments seti-ui monokai-seti git-blame custom-title local-history split-diff

# Seti UI: Compact Mode: True
# Custom Title Settings > Template: <% if (fileInProject) { %><%= relativeFilePath %> | <%= projectName %><% } else { %><%= filePath %><% } %>
